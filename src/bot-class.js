const Bot = class Bot {
    constructor(name, urlPicture, newMessages) {
        this.name = name;
        this.urlPicture = urlPicture;
        this.newMessages = newMessages;
    }
};

export default Bot;